from pathlib import Path

CONFIG_FILE_PATH = Path("config/config.yaml")
# While params.yaml is empty it will return error
# So mention anything inside params.yaml file
PARAMS_FILE_PATH = Path("params.yaml")